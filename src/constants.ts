export const DEBOUNCE_TIME = 1000;
export const COVER_IMAGE_REGEX =
  /^https?:\/\/([\w\d-]+\.)*[\w\d-]+\.[a-z]+(?:\/[^\s]*)?$/i;
export const SEARCH_QUERY_REGEX = /search=([^&]*)/;
export const AUDIO_SIZE_LIMIT = 10 * 1024 * 1024; // 10MB
